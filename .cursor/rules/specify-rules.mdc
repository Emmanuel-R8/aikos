# maiko Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-04

## Active Technologies
- Python 3.12 (for orchestration scripts); existing C, Zig, + Standard library only for orchestration (subprocess, (001-multi-impl-parity)
- Files on disk in the repository (JSON state, JSONL logs, trace (001-multi-impl-parity)

- Shell scripts (bash/sh), compatible with existing Medley build infrastructure
- File system (build artifacts, configuration files)
- Shell scripts (bash/sh), compatible with existing Medley script infrastructure
- File system (lock files for concurrent run prevention, configuration via environment variables)
- Bash 4.0+ (POSIX-compliant shell scripting)
- File system (lock files, emulator executables)
- None (ephemeral log files + a per-user lock file)
- Shell (bash + POSIX sh), C (Maiko), Zig 0.15.2+, Python 3

- Markdown with Mermaid diagrams / Documentation format (001-emulator-rewrite-docs)

## Project Structure

```text
maiko/src/
tests/
```

## Commands

# Add commands for Markdown with Mermaid diagrams / Documentation format

## Code Style

Markdown with Mermaid diagrams / Documentation format: Follow standard conventions

## Recent Changes
- 001-multi-impl-parity: Added Python 3.12 (for orchestration scripts); existing C, Zig, + Standard library only for orchestration (subprocess,

- Added Shell (bash + POSIX sh), C (Maiko), Zig 0.15.2+, Python 3
- Added None (ephemeral log files + a per-user lock file)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
