# maiko Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-04

## Active Technologies
- Shell scripts (bash/sh), compatible with existing Medley build infrastructure (003-unified-build-system)
- File system (build artifacts, configuration files) (003-unified-build-system)
- Shell scripts (bash/sh), compatible with existing Medley script infrastructure (004-emulator-runner)
- File system (lock files for concurrent run prevention, configuration via environment variables) (004-emulator-runner)
- Bash 4.0+ (POSIX-compliant shell scripting) (004-emulator-runner)
- File system (lock files, emulator executables) (004-emulator-runner)
- None (ephemeral log files + a per-user lock file) (004-emulator-runner)
- Shell (bash + POSIX sh), C (Maiko), Zig 0.15.2+, Python 3 (004-emulator-runner)

- Markdown with Mermaid diagrams / Documentation format (001-emulator-rewrite-docs)

## Project Structure

```text
maiko/src/
tests/
```

## Commands

# Add commands for Markdown with Mermaid diagrams / Documentation format

## Code Style

Markdown with Mermaid diagrams / Documentation format: Follow standard conventions

## Recent Changes
- 004-emulator-runner: Added Shell (bash + POSIX sh), C (Maiko), Zig 0.15.2+, Python 3
- 004-emulator-runner: Added None (ephemeral log files + a per-user lock file)
- 004-emulator-runner: Added Bash 4.0+ (POSIX-compliant shell scripting)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
